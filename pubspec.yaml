name: s_p
description: Sistema de mapeamento da violência em Manaus

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_maps_flutter: ^2.1.1
  google_maps_flutter_web: ^0.3.2
  geolocator: ^8.0.1
  geocoding: ^2.0.1
  dio: ^4.0.4
  http: ^0.13.4
  pausable_timer: ^1.0.0+3
  syncfusion_flutter_datepicker: ^19.4.38
  bottom_picker: ^1.4.0
module:
  androidX: true

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true

  assets:
    - assets/crime_icon.png
    - assets/weapon.png

